using System;
using System.Collections.Generic;
using System.Diagnostics.Metrics;
using System.Linq;
using System.Reflection;
using System.Text;
using System.Threading.Tasks;
/*
namespace ClassExerciseNumbers
{
    internal class Vowels
    {
       private static bool IsAVowel(char letter)
        {
            char[] vowels = { 'a', 'e', 'i', 'o', 'u' };
            for (int index = 0; index < vowels.Length; index++)
            {
                if (vowels[index] == letter)
                {
                    return true;
                }

            }
            return false;*/


            /*public static string RemoveAllTheVowels(string letters);
            {
                string returnString = "";
                for (int index = 0; index < letters.Length; index++)
                {
                    char letters = letters[index];
                    bool vowel = IsAVowel((char)letter);
                    if (!vowel)
                    {
                        returnString += letter;
                    }

                    return returnString;
                }
            }*/
    //    } 
      /*  public static string RemoveAllTheVowels(string letters);
            {
                string returnString = "";
                for (int index = 0; index<letters.Length; index++)
                {
                    char letters = letters[index];
        bool vowel = IsAVowel((char)letter);
                    if (!vowel)
                    {
                        returnString += letters;
                    }

                    return returnString;
                }
            }
    } }
      */